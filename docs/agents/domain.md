# Domain documentation

This is a single-context repository.

Before exploring code, read root `CONTEXT.md` and any relevant decisions under `docs/adr/`. Missing ADRs are not an error and should not be created speculatively.

Use the canonical terms from `CONTEXT.md`. Surface conflicts with an existing ADR instead of silently overriding it.
